<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Cocktail List</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>

<div class="container my-4 mb-2">
    <h1>Cocktail List</h1>
  <%= paginate @cocktails %>
    <div class="row font-weight-bold border-bottom pb-2">
        <div class="col-2">Image</div>
        <div class="col-2">Name</div>
        <div class="col-2">Category</div>
        <div class="col-2">Glass</div>
        <div class="col-4">Instructions</div>
    </div>

    <% if @cocktails.any? %>
        <% @cocktails.each do |cocktail| %>
            <div class="row py-3 border-bottom align-items-center">
                <!-- Image column -->
                <div class="col-2">
                  <img src="<%= cocktail['strDrinkThumb'] %>" alt="<%= cocktail['strDrink'] %>" class="img-fluid" style="width: 100px; height: auto;">
                </div>

                <!-- Cocktail details-->
                <div class="col-2"><%= cocktail['strDrink'] %></div>
                <div class="col-2"><%= cocktail['strCategory'] %></div>
                <div class="col-2"><%= cocktail['strGlass'] %></div>
                <div class="col-4"><%= cocktail['strInstructions'] %></div>
            </div>
        <% end %>
    <% else %>
        <div class="row py-3">
            <div class="col text-center">
                <p>No cocktails available currently.</p>
            </div>
        </div>
    <% end %>
</div>

<script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" ></script>

</body>
</html>
